export default function HeroDashboard() {
  return (
    <img
      src="/assets/img/hero-visual.png"
      alt="AI trading dashboard showing a candlestick chart with an upward trend, plus pattern recognition and risk reminder notes"
      width={1040}
      height={940}
      loading="eager"
      decoding="async"
      fetchPriority="high"
      className="h-auto w-full max-w-[520px]"
    />
  );
}
